# Hermes 使用说明

```bash
npx niantou-tongda-skill install --target hermes --scope user
hermes skills list
hermes chat --toolsets "skills,terminal"
```

如果 project scope 安装，需要在 Hermes 配置中加入项目 skills 路径。
