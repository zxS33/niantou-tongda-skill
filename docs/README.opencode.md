# OpenCode 使用说明

OpenCode 可同时使用 skills 和 commands。

```bash
npx niantou-tongda-skill install --target opencode --scope user
```

安装后可使用：

```text
/niantou-tongda
/tongda-workflows
/pojing-breakthrough
/engineering-execution
```
