# nanobot 使用说明

`niantou-tongda-skill` 在 nanobot 中以 workspace skills 方式接入。

## 安装

```bash
npx niantou-tongda-skill install --target nanobot --scope user
```

默认复制到：

```text
~/.nanobot/workspace/skills
```

新会话中 nanobot 会发现 `niantou-tongda` 和下游 skill。
