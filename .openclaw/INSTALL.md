# OpenClaw 使用说明

`niantou-tongda-skill` 可作为 skills 目录安装，也可由兼容 Claude/Cursor bundle 的宿主读取。

## 安装

```bash
npx niantou-tongda-skill install --target openclaw --scope user
```

默认复制到：

```text
~/.openclaw/skills/niantou-tongda-skill
```

## 验证

新开会话后确认 `niantou-tongda` 入口 skill 可被发现。
